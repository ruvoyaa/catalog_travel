# Catalog Travel Context

This project was initialized from the shared `job` workspace rules.

Current startup baseline:

1. stack: Laravel / PHP
2. startup mode: analysis, then development
3. expected scale: medium, but with reinforced documentation
4. frontend: Vue + Vike + Vite + Tailwind 4
5. database: PostgreSQL
6. infrastructure: monorepo + Docker
7. admin UI placement: Laravel backend

Working priorities:

1. factual repository state
2. user startup prompt
3. local project rules and docs
4. shared rules from `/home/ruvoyaa/projects/job`

Notes:

- do not invent architecture before the task is clarified;
- first stage is documentation foundation;
- current plan should stay within 3-4 substantial stages;
- booking functionality is explicitly out of scope.
